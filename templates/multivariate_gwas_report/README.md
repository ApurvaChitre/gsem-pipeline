# multivariate_gwas_report template

This folder is copied into `MULTIVAR_REPORT_DIR/` by `slurm/prepare_report.sbatch`.

The notebook (`multivariate_gwas_report.ipynb`) was copied from the current TSCC workflow and may contain:
- hard-coded paths from an example run
- cached output cells

Recommended:
1. Open the notebook and set/update the “base directory” variable(s) near the top to point to your `MULTIVAR_REPORT_DIR`.
2. Clear outputs (optional) and rerun.
